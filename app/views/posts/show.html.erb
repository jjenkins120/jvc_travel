<h1>Posts</h1>


<div>
    <p>User <strong><%=link_to @post.user.name, user_path(@post.user.id)%></strong></p>
    <p>Title <strong><%=@post.title%></strong></p>
    <p>Description <strong><%=@post.description%></strong></p>
    <p>Likes <strong><%=@post.likes%></strong></p>
    <p>ID <strong><%=@post.id%></strong></p>
    <p>City <strong><%=@post.trip.destination.city%></strong></p>
    <p>Country <strong><%=@post.trip.destination.country%></strong></p>
    <p>Comments: <strong><%@post.comments.each do |comment|%></strong><br>
        <strong><%="#{comment.description} - #{comment.post.trip.user.name}"%></strong><br>
        <% if comment.post.trip.user == current_user  %>
             <button>  
                <%= link_to "Edit this Comment", edit_comment_path(comment) %>
            </button>

            <button>  
                 <%= link_to "Delete this comment", comment, method: :delete, data: { confirm: "Are you sure you want to delete this comment?" } %>
            </button>
        <% end%>
        <% end%>  
</div>


<!--
   <% form_for @comment do |f| %>
   <%= f.label :description%>
   <%= f.text_field :description %>
   <%= f.submit %>
   <% end%>
-->

<% if @post.user == current_user %>
    <button>  
        <%= link_to "Edit Post", edit_post_path(@post) %>
    </button>

    <button>  
        <%= link_to "Delete Post!", @post, method: :delete, data: { confirm: "Are you sure you want to delete your Post?" } %>
    </button>
<% end%>



